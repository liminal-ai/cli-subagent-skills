# Cursor Subagent Skill

This package is reserved for the future `cursor-subagent` implementation.

The folder exists now so the monorepo already has a stable place for Cursor alongside Claude Code, Codex, and Copilot.

Implementation is intentionally deferred until the runtime contract and packaging details are ready.
